/**
 * Order service module
 * Re-exports from orderService.core.ts for backward compatibility
 */
export { createOrderService, type IOrderService, type SupabaseClientLike } from './orderService.core';

